.*((\(([0-9]{3,4})\)\. ))   //(2001). 으로 끝나는 앞문자열 매칭
(?<=\.).+ //.으로 끝나는 것 뒤로 다 선택
(?<=“).+(?=”) //“ ”사이에 있는 값을 추출
(?=”).+ //”를 포함한 뒤에 모두 선택
.+(?<=“) //를 포함한 앞에 모두 선택 

.+(?<=")
(?=").+



// 검색할때 콘솔창에 나오는 항목 전거하기 위한 정규식
^.*(SEARCH) @ 